import {cleanup,render,screen} from "@testing-library/react";import {afterEach,beforeEach,expect,it,vi} from "vitest";import {CorporateUnresolvedComponents} from "./corporate-unresolved-components";const fetchMock=vi.fn();beforeEach(()=>{fetchMock.mockReset();vi.stubGlobal("fetch",fetchMock);});afterEach(()=>{cleanup();vi.unstubAllGlobals();});
it("shows FPC as visibly unresolved without activation controls",async()=>{fetchMock.mockResolvedValue({ok:true,json:async()=>({components:[{component_key:"hfo-al-m09-02",source_id:"AL-M09",label:"FPC EOM review",mapping_state:"unconfirmed",subject_state:"unconfirmed",source_state:"unavailable",reason:"Meaning and source remain unconfirmed.",can_register:false,can_prepare:false}]})});render(<CorporateUnresolvedComponents authorized/>);expect(await screen.findByText(/FPC EOM review · Mapping: unconfirmed/)).toBeVisible();expect(screen.getByText(/Registration and preparation are unavailable/)).toBeVisible();expect(screen.queryByRole("button")).toBeNull();});
it("does not request or render the organization surface for a site role",()=>{render(<CorporateUnresolvedComponents authorized={false}/>);expect(fetchMock).not.toHaveBeenCalled();expect(screen.queryByText(/FPC/)).toBeNull();});
