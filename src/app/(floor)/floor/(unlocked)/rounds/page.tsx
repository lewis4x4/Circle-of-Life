import { FloorRoundsScreen } from "@/components/floor/FloorRoundsScreen";

/** `/floor/rounds`: the building's rounding queue in the tablet shell (spec 40 §6 screen 7). */
export default function FloorRoundsPage() {
  return <FloorRoundsScreen />;
}
