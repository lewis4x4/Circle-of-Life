import { FloorSetupForm } from "@/components/floor/FloorSetupForm";

/** `/floor/setup`: enroll this iPad as a floor tablet (spec 40 §4, §6). No session. */
export default function FloorSetupPage() {
  return <FloorSetupForm />;
}
