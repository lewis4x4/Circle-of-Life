/**
 * Audit-defanged: was a giant translucent `text-9xl/text-[160px]` background
 * watermark of the numeric value behind every V2Card. Renders nothing.
 * Signature kept so consumers continue to compile.
 */
export function MonolithicWatermark(_props: {
  value: string | number;
  className?: string;
  containerClassName?: string;
}) {
  void _props;
  return null;
}
