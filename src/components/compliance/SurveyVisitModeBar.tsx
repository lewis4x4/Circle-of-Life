"use client";

/**
 * @deprecated Survey visit controls moved to workspace header —
 * {@link SurveyVisitWorkspaceToggle} / {@link SurveyVisitWorkspaceDock} in {@link AppShell}.
 */
export function SurveyVisitModeBar() {
  return null;
}
