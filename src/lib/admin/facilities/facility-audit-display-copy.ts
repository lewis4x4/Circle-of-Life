/**
 * Quiet Operator copy for facility audit log JSON cells.
 * Missing values name real gaps — never fabricate prior or new field text.
 */

export const FACILITY_AUDIT_NO_VALUE_COPY = "No value posted";
