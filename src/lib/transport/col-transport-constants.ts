/**
 * COL transport economics — flat trip fee alternative to mileage reimbursement.
 * See `organization_transport_settings.flat_trip_fee_cents` (migration 144).
 */

export const COL_FLAT_TRIP_FEE_CENTS = 75_00;
