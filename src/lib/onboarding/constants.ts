/** Default COL org from `008_seed_col_organization.sql` — used when JWT `onboarding` users lack `user_profiles.organization_id`. */
export const COL_DEFAULT_ORGANIZATION_ID = "00000000-0000-0000-0000-000000000001" as const;
