/**
 * Quiet Operator copy for survey visit header chrome (AppShell / facility detail).
 * Hydration keeps the idle accessible name — never "Loading…" in operator chrome.
 */

/** Accessible name for the survey visit context control (icon trigger). */
export const SURVEY_VISIT_CONTEXT_ARIA_LABEL = "Survey visit context";

/** Tooltip for the survey visit context icon — same while hydrating and when resolved. */
export const SURVEY_VISIT_CONTEXT_TOOLTIP =
  "Survey visit mode — activate when a surveyor is on site. When active, log chart pulls for the survey trail.";

/** Visible label beside the icon on facility detail header actions. */
export const SURVEY_VISIT_FACILITY_HEADER_LABEL = "Survey visit";
