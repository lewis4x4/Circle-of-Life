{
  "name": "facility-launch-center",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Facility Launch Center for Homewood launch readiness.",
  "scripts": {
    "check": "find src scripts -name '*.js' -o -name '*.mjs' | sort | xargs -n1 node --check",
    "smoke": "node scripts/smoke-static.mjs",
    "verify": "npm run check && npm run smoke && node scripts/verify.mjs",
    "serve": "python3 -m http.server 8080"
  }
}
