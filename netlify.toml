[build]
  base = "facility-launch-center"
  command = "npm run verify && rm -rf dist && mkdir -p dist && cp index.html styles.css robots.txt _redirects dist/ && cp -R src dist/src"
  publish = "dist"
