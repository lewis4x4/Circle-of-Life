# Haven independent-review completion plan

Resumed 2026-09-07 at eb1d98f1 on codex/haven-review-sys-001. User authorizes completion, commits, merge to main, push, and a fresh comprehensive review. Original 119-entry dispositions do not close the subsequent 48 findings.

1. Finish and independently review SYS-001E lifecycle, durable retries, current client authority and migration327. Preserve clinical/staff/audit identity. Consolidated SQL, unit, lint, typecheck, production build and UI gates precede commit/push.
2. Close remaining security/integrity findings, then frontline producers and context/clinical lifecycle gaps, navigation and full reporting, business/office/support/operations journeys. Reconcile each retained finding against concrete source and behavioral proof. One bounded integrated segment and atomic commit at a time.
3. Reconcile the exact GitHub, local, Netlify, migrations and Edge release lineage. Do not rewrite applied migration history or touch unrelated local work. Hosted schema/API parity and signed-role checks are separate from local replay.
4. Independently review the integrated product using original coverage plus changed workflows, negative authorization, failure/retry, concurrency, refresh and downstream receipt checks. Fix every actionable regression found and rerun affected gates.
5. Merge and push verified commits to main; verify CI/deployment state and report remaining external acceptance inputs honestly. Owner/staff operational UAT cannot be invented by engineering.

## Verification and artifact handling

Durable evidence belongs here and in test-results/agent-gates. Disposable artifacts belong only beneath the private manifested run directory ~/.hermes/tmp/agent-runs/haven-r7-01a07c39; only exact current-run artifacts may be cleaned through jarvis-storage-steward. Existing uncommitted SYS-001E work is preserved and completed. Other worktrees, unrelated brand files, historical logs and unknown-owned resources are retained.

## Cleanup/simplification approach

Use regression coverage for behavior before altering lifecycle orchestration; reuse existing authoritative actor guards and domain operations; eliminate stale JWT authority and ad hoc duplicated Auth-sync sequences only after their replacements are tested. No dependency additions or broad stylistic refactor.

Mission alignment: risk until security/integrity fixes and their matched runtime verification are complete.

## User stop boundary

The user subsequently instructed: finish the current task and stop before the next one. The active task is SYS-001 authorization/lifecycle completion, including independent-review corrections, SYS-008 routed revocation overlap, verification and commit/release steps. The rest of the 48-finding roadmap is retained in roadmap-status.json and is not being started in this continuation.
