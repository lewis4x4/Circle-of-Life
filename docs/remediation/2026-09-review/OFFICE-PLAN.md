# Office remediation lane plan

Scope O01-O11 except O06 (lead owns navigation). Preserve the existing OCE task engine, private workspace pages, version history, and uploaded file bytes.

1. Add regressions for false Drive completion and Eastern window bounds before correcting helpers.
2. Give meeting actions one transactional creation identity and synchronize status with OCE; preserve dirty minutes during related refreshes.
3. Save workspace versions and team/first-lead membership atomically with explicit conflicts.
4. Expose the existing versioned required-reading contract to floor roles and count only selected-facility staff.
5. Label bookmark inventory separately from verified content cutover; reconcile row counts. Make file removal reversible without guessing permanent retention policy.
6. Run focused tests/lint/typecheck and executable SQL fixtures through local replay when its runtime is available. No external messages or production mutations.
