# Finance integration and platform audit test plan

Status: PLANNED, NOT EXECUTED. No acceptance is passed by this document, inventory scanner, test name or proposed command. Preserve all 79 source IDs and source/local-SQL/full-Auth/API/Edge/browser/provider/live/business evidence distinctions.

The F00 command contract is `npm run test:finance-integration` and `npm run test:platform-audit`. The implementation owner must bind these scripts to real behavior assertions and machine-readable case results; this plan does not claim the scripts already exist or have passed. JSON results need case/acceptance IDs, expected versus actual assertions, counts, skips/failures, exact command/exit, UTC timing, source commit and dirty-diff identity, target/environment, fixture seed/hash and sanitized evidence references. Planned IDs cannot appear as passed test results.

Required fixture: two organizations, three legal entities, two facilities sharing one entity, restricted facility admin, scoped accountant/auditor, independent preparer/approver, active/revoked users and historical residents/transfers. Include >2,000 rows, >1000 configured REST cap, an artificially lower page cap, >100,000 rows for proposed volume budget and aggregate cents >2^31. Calculate expected money independently of implementation functions with exact integers; do not make tests reproduce posting code. Add the actual approved portfolio mapping separately.

| Phase | Acceptance coverage | Required behavior and evidence layer |
|---|---|---|
| F00 | HFA-001, HFA-002, HFA-003, HFA-004, HFA-005, HFA-006 | Source/target provenance, complete declared-and-traced audit inventory, scoped entity/product inventory, approved policy/operating targets; distinguish missing human approval from completed local structure. |
| F01 | HFA-007, HFA-008, HFA-009, HFA-010, HFA-011, HFA-012 | SQL transaction failure injection, concurrent/repeated/conflicting receipt/allocation and journal operations; current/revoked actors; wrong organization/entity/account; closed-period bypass; authenticated audit-export facility scope reproduction and negative tests. |
| F02 | HFA-014, HFA-015, HFA-016, HFA-017, HFA-018, HFA-019 | Canonical funds debit/credit/refund/transfer/overdraw; no automatic trust-to-AR netting; bank/liability three-way tie-out; opening AR cannot create revenue; repeated/quarantined historic imports and reviewed cutover. |
| F03 | HFA-013, HFA-020, HFA-021, HFA-022, HFA-023, HFA-024, HFA-025, HFA-026, HFA-027, HFA-079 | Source+receipt+outbound atomic commit, immutable member uniqueness, payload/hash approval invalidation, independent approval, bounded worker leases/order/retry, signed/deduped/reordered inbox, reconnect/revocation, stop controls, privacy allowlist. Pre-dispatch protected recovery evidence, lost acknowledgment/metadata/keys and recovery store outage must block dispatch. |
| F04 | HFA-028, HFA-029, HFA-030, HFA-031, HFA-032, HFA-033 | Selected actual product controlled sandbox/test company/file workflow; amount/account/basis capabilities, provider readback, expired catch-up/full snapshots, wrong-company reconnect, quarantine/deduplication and UI truthful capability states. |
| F05 | HFA-034, HFA-035, HFA-036, HFA-037, HFA-038 | Approved summary journals with independent debit/credit/account oracle; opening/control/clearing/deferred balances; no detail+summary/native+mirror double counting; exact decimal boundaries; immutable linked corrections; source-member/provider tie-out. |
| F06 | HFA-039, HFA-040, HFA-041, HFA-042, HFA-043, HFA-044 | Shared MCO deposits across residents/payers, concurrent/partial/unapplied receipts, concessions/retroactive rates, bed hold/discharge, denial/short-pay/reversal, refund/NSF/fees/settlement. Run independent expected charges through both application and Edge monthly producers including catch-up/repeat/DST. |
| F07 | HFA-045, HFA-046, HFA-047, HFA-048, HFA-049, HFA-050 | PO/receipt/invoice/payment chain; vendor duplicate and bank-change permissions; rejected/amended payroll, accepted actual results, hours/mileage duplicate channels; capital/prepaid/shared cost and intercompany one-sided failure/elimination. |
| F08 | HFA-051, HFA-052, HFA-053, HFA-054, HFA-055 | Opening+movement=closing independently; >caps, failed/short pages and missing source; concurrent insert/update/delete including same-count replacement; immutable saved membership; stale entity status, assigned unmatched amount/count and bank matching without new revenue. |
| F09 | HFA-056, HFA-057, HFA-058, HFA-059, HFA-060, HFA-061, HFA-062 | Per-register-row enforced allowed/denied read/search/export/download/write/replay at page, PostgREST/RPC/server/Edge/storage/job paths; no secrets; protected receipts; privileged tamper/missing segment detection; formula-safe complete scoped export and retrieval revocation; hold/attachment/key recovery; clinical audit-outage continuity. |
| F10 | HFA-063, HFA-064, HFA-065, HFA-066, HFA-067 | Authenticated realistic role journeys, entity/facility switch, close/post races and reopen, no clinical drill-down for finance-only actors, empty/error/populated states, WCAG AA/keyboard/reduced motion, AI citations/injection/tool restrictions and deterministic fallback; offline account switch/revocation/replay. |
| F11 | HFA-068, HFA-069, HFA-070, HFA-071, HFA-072 | Explicit required test layers without relevant skips, exact-source independent reviews, load/chaos/alerts, restore before source creation and before provider posting with workers paused and protected metadata recovery, compatible release/rollback rehearsal. |
| F12 | HFA-073, HFA-074, HFA-075, HFA-076, HFA-077, HFA-078 | Verified hosted schema/runtime/worker/Auth/PostgREST/browser targets, read-only shadow/opening controls, authorized scoped batch independent production readback, seven clean days plus real billing/payroll/month-close, every entity first close, named trained operators/acceptors. |

SQL probes must use `review_*.sql` (or explicitly register another name) because `scripts/pg-verify-migrations.mjs:56` and its Docker path select those probes plus three explicit legacy tests. Require `REQUIRE_PG_VERIFY=1`; a native PostgreSQL run with auth stubs is not real Supabase Auth/PostgREST. Use disposable local/test targets; source-replay SQL redefining auth helpers must never target production. Inspect each command for side effects and target destinations before execution.

Required existing checks: `npm run migrations:check`, `npm run typecheck`, `npm run lint`, `npm test`, `REQUIRE_PG_VERIFY=1 npm run migrations:verify:pg`, `npm run build`, then the phase segment gate once and its UI gate only for UI scope. Add changed Edge tests, full local authenticated API/browser/negative authorization tests, privacy and provider contract/chaos/recovery tests explicitly. `.github/workflows/ci-gates.yml` conditionally skips several checks under `HAVEN_UI_GATES_ENABLED`; workflow success alone is not execution evidence. PR deterministic checks must run without provider secrets; selected-provider and live evidence runs remain separate required gates.

Provider runner contract: read-only by default, verified environment/company identity before access; writes require explicit allowed command IDs and existing activation authority, never broad test writes. File/manual adapters require actual edition-specific exchange, captured posting acknowledgment and independent readback. New payment initiation, payroll submission and outbound messages remain disabled. Safe repository artifacts use hashes/reference IDs, never credentials or resident content.

AUDIT-COVERAGE.json starts with conservative source candidates. Add per-row executed case IDs, verified current scope rule, event sink, actor/reason/version semantics and evidence of alternate-path denial or auditing. Trace pages through shared components/hooks/helpers and dynamic SQL/RPC paths before claiming inventory completeness. Candidate counts and generated JSON validation only prove artifact structure. Independent security review must challenge direct data access and null-facility/service-role boundaries.

Calendar-dependent live gates cannot be simulated away: record real cycle dates and per-entity named acceptance when available. Keep HFA-061 and HFA-073–078 open until their appropriate policy/live/business evidence exists. HFA-069 requires an independent reviewer of the exact release, not the earlier roadmap APPROVE.

## Upstream and hosted refresh — 2026-09-08

The original handoff/source review remains `3dec84bbdc216d1c58e7296568b007ebe6f101be`. The implementation lead verified newer main/deployed source `fad17dcc4bd2dc1deb35400b309c65a047b8de83` and integrated it at `63e8909a6b17ed11a398052d5baa4de6eec2aeba`. This refresh reads the committed integration delta; in-progress finance 336/audit 337 and other uncommitted root edits remain provisional and are not acceptance evidence. `HOSTED-MIGRATIONS.json` records read-only SQL against `manfqmasfqppukpobpld` at `2026-09-08T23:00:30.493Z`: 344 migration-history records, with numbered sequence through 335 plus timestamped records. Migration-history presence is not runtime, authorization, payroll-provider or finance reconciliation proof.

Upstream `supabase/migrations/334_payroll_source_freshness.sql` adds punch source revisions, source freshness checks, serialized payroll writes, immutable historical exported lines, editable-draft refresh/exclusion commands and guarded snapshot export. Current `src/app/(admin)/payroll/[id]/page.tsx` calls `refresh_payroll_time_records`, `exclude_payroll_draft_punch` and `payroll_export_snapshot`; CSV comes from the returned snapshot and verifies line count. Reuse these controls and test their current behavior; do not reconstruct a stale client-only payroll architecture from older module documents. Generic CSV still does not prove actual ADP company binding, proprietary-template acceptance, payroll actuals or settlement. Historical exported evidence is retained, with the UI explicitly noting that legacy batches lack an original file archive.

Upstream `supabase/migrations/335_employee_file_lifecycle.sql` introduces six employee-file tables (requirements, medical access, records, signatures, duty events and audit events), scoped command/helper functions, personnel/medical storage boundaries and immutable history. Actual routes include `/employee-file`, `/employee-file/reviews/[id]`, `/admin/staff/[id]/employee-file` and the staff employee-file API/catalog/requirements/training/download handlers. The download handler records `record_download` through `haven_employee_file_command` before requesting a 60-second signed URL. This is an existing implementation to inspect and extend, not proof every direct storage/SQL/read path is audited. Employee medical evidence must remain separately scoped and confidential payloads excluded from general audit history.

Refresh-specific regression obligations: amended punch invalidates nonhistorical export; queued/exported transitions revalidate source state; draft refresh does not steal a punch owned by another batch; excluded lines and historical exported payloads remain traceable; snapshot count and scope must match. Exercise medical versus personnel grants, medical-access revocation, requirement version/signature immutability, training-evidence reuse, duty events, direct PostgREST/RPC/storage bypass, failed download-event capture and signed URL lifecycle. Include current upstream employee-file unit/API tests and SQL probes as executed dependencies, not assumed green upstream evidence. Keep all HFA audit requirements open until the full per-entry-path tests run.
