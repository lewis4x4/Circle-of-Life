# Finance build resumption

Overall task remains IN_PROGRESS. No full phase, engineering-complete or goal-complete claim.

Workspace: `/Users/brianlewis/Circle of Life/Haven Finance Integration`
Branch: `codex/haven-finance-integration`
Refreshed upstream main: `fad17dcc4bd2dc1deb35400b309c65a047b8de83`; integration merge `63e8909a6b17ed11a398052d5baa4de6eec2aeba`. Original source checkout and seven unrelated untracked files remain byte-identical.

Reviewed initial source scope: F00 registers/payload/tests/CI/security compatibility, F01 migrations336/337 and callers, F02 migration338 and current resident-money reads. Independent source approvals are in INDEPENDENT-REVIEWS.json and the task history. Native341 migrations/22 probes, full3473 unit assertions with0 skips, source typecheck and source/public-shell gates pass. Actual owner login and empty trust page rendered through real local Auth. Scoped authenticated axe still failed at its unchanged20-second load budget; it is not waived.

Run-owned local test infrastructure: `~/.hermes/tmp/agent-runs/hfa-20260908-01a08335/`. Native PostgreSQL17 uses its Unix socket on55447. Full Supabase project is `hfa-01a08335`; Docker DNS and engine operations are intermittent. Only this run's containers were changed. Real Auth helpers were not replaced with stubs in that stack. Private connection/credential files are beneath `full-supabase/` and must not be committed. Preview is `http://127.0.0.1:4317`; proxy59331 reaches actual local services, standaloneDeno browser gateway59332 adds the exact audit handler. Check current health/config before resuming, since isolated Auth repair is ongoing.

Initial checkpoint is `1e0a36eec5542d537b15c20f20b50b533d819a82`, pushed in draftPR461. F03 staging source is independently approved and copied into the repository: migration339, review_finance_event_outbox.sql, F03-EVENT-CONTRACT.md. Final migration hash `ffb4f41d3454b4d8d89c90be538edeef894b8689ce0a16ecee9e431f602e65b3`;56 new assertions,342 total migrations/23 probes and controlled races passed. It keeps payloadNULL/dispatchfalse. It does not implement full approval, provider binding, dispatcher, recovery or later phases.

Next executable work: finish or accurately block authenticated API/browser evidence; finish remaining F03 approval/member/connection/recovery work; implement remaining F03 approval/member/recovery infrastructure and continue independent later engineering. Maintain all79 IDs. Do not classify opening records, mark providersN/A, activate production, or claim live acceptance without required evidence.

Missing business facts are already asked once: accounting products/editions/company files per active entity, actual payroll provider and controller/accountant. Named privacy/book/cutover policies, recovery targets and activation authority remain open in DECISIONS.md. A sharedDockerrestart would interrupt unrelated projects; approval question is pending, and no sharedrestart is authorized yet.

Final local runtime coverage is recorded in test-results/finance-integration/authenticated-runtime-blocker.json. Three synthetic roles obtained real GoTrue sessions and a scopedREST query returned1000 of2505 rows. The complete negative/export/revocation/paymentAPI suites did not complete. StandaloneDeno testing is not deployed gatewayJWT proof. Source-only runner validation does not close these gates.

Additional reviewed commits: 250f22ff repairs CI scanner/date portability; 0c24ba11 commits339 staging; b7628650 commits safe Edge denials and blocked authenticated probe evidence; e25089f3 repairs Cash response scope; b2582e83 repairs deterministic employee catalog-test request targeting. CI run34302861337 passed at250f22ff (through338); run34303020164 failed one catalog test atb7628650, now repaired. Latest-head CI must be verified separately. Full local unit run withCash cases passed3481 with0skips; full lint passed.

Cleanup validation for downloaded CI250f22 artifacts failed closed with Storage Steward StateSafetyError; copies are retained under their exact private manifest. Native test environments and credentials remain retained for active verification.

341 discovery and readonly workbench are finalized with independent source/native/fixture approvals. HFA-F03-WORKBENCH gate passed; full3557unit cases passed; source/component/hash evidence is in f03-workbench. Fixture preview is http://127.0.0.1:5186 (synthetic Auth/RPC, intentionally not a real route/Auth proof). Actual localSupabase remains unreliable.

Current committed source is4c13470c496b5608110021f266584f812dcd66c2. Unconditional Finance CI34312150532 passed at that exact commit; step metadata is in f03-workbench/ci-4c13470c.json. Public workbench routes remain source-only with no real Auth/routed acceptance.

The next reviewed source segment is the private QBO webhook authentication/schema helper plus test runner and CI hook.24 Deno cases and314 independent corpus cases passed, alongside audit15 regression and HFA-F03-WEBHOOK gate (344 migrations/25 SQL probes). It does not implement a receiver, durable inbox, tenant binding, ACK or catch-up.

342 credential lifecycle is integrated at migration SHA dc62a329bf5b464d486bde0213633120f3d835d56000a71ff419309919e553f8 and probe SHA cf44f4f9893eba28432bd23e2122f18b716a9d432d6de5dcd08d15945d8992b8.83 native cases,10 independent lifecycle probes and7 metadata races pass. HFA-F03-CREDENTIALS gate passed with345 migrations/26 probes; the exact source has lifecycle-only APPROVE. Source, evidence and metadata are under f03-credentials. The independent dynamic security review was blocked by an automatic safety filter citing possible cybersecurity risk; no final security verdict exists. Real Vault encryption, issuer/gateway, provider, restore and production gates remain open. Webhook CI34317877537 passed at5c24e1d5;342 requires its own CI.

Next source: finance_source_inventory owns bounded F06 billing guard repairs, with f00_independent_review preparing independent cases. Baseline28 failures were reproduced privately: draft schedules may be selected, material queries silently truncate, and the actual Edge organization handler references undefined max_facilities. Implement explicit incomplete-source blocking and correct draft/handler behavior without changing charge policy or claiming complete snapshots. Root owns shared runners/registers/commits. No343 migration is reserved. All remaining F03 provider/inbox/recovery and later phases remain unfinished.

Local Docker version query answered, but DB/Auth/REST/Kong inspection timed out and a disposable replay container creation stalled. Root stopped only the exact own command client. Container haven-pg-verify-1788934272738 creation/cleanup state is UNKNOWN and retained in runroot/docker-replay-runner/manifest.private.json. No shared Docker restart or unrelated deletion occurred. Native PostgreSQL remains usable. Docker replay now uses an explicit unique haven_verify_* database to preserve the credential test guard; actual Docker proof depends on the next CI run.

Vault0.3.1 availability was verified through read-only hosted catalog metadata. Service_role has Vault schema/decrypted-view access; it is not a scoped worker. Actual localVault catalog query timed out; encryption/rollback proof is open. Current official QBO research identifies refresh-response loss as potentially requiring reconnect; do not assume24h overlap guarantees recovery. Research and catalog metadata are preparation, not provider proof.

F06 source guards are frozen and independently approved at app8bb8f636 / Edge93485790 / handler98778264. HFA-F06-SOURCE-GUARDS passed; full unit3665/3665 with zero skips, focused finance225, author119 and independent131 passed. Original skipped-benchmark report remains evidence of an incomplete attempt. Next temporal payer selection work is private with finance_source_inventory; do not simply filter all payers at month start because mid-month admissions need their actual billable window. Native Auth/API evidence is separately under review by f00_independent_review.

Native runtime now provides independent real Auth/API proof at loopback59831 and actual standalone Deno59835, PG socket native-services/port55448/databasehfa_native_01a08335. HFA007/HFA012 engineering PASS is supported by final26audit/25finance/12HTTP/29identity/34followup assertions plus existing SQL. All keys/configuration remain private. Native preview http://127.0.0.1:4327 is active for browser work; initial shell-only capture is not acceptance. Verified hosted SELECT grants for11 platform/RLS-dependency tables were applied only to this native browser fixture after the API schema-parity review; record that overlay separately. Root owns native runtime/driver/browser artifacts; no shared Docker restart occurred.

Temporal payer source is finalized at appdf86fbcd / Edge09bc6237 / test1bb5102d with independent APPROVE; full3712/3712, focused272 and HFA-F06-TEMPORAL pass. Next F06 architecture calls for database-materialized billing runs with immutable source/candidates, source fences and resumable per-item receipts; a read-only architecture proposal is in task history. No343 reservation yet. QBO read transport remains uncommitted under review with reproduced deadline/cancellation/schema defects being repaired.

## Active execution checkpoint

Latest committed/pushed source: ab9296b529d766d4fa92eb3857ca7f74bf10d627. Finance CI34355907333 passed at that commit (native proof38401797 and source guardsd1023d14 also passed their exact CI). Full temporal regression3712/3712, focused272; no full phase or goal completion. Goal tool confirms the full end-to-end objective remains active and unbounded.

Root currently owns two uncommitted UI fixes: AppShell.tsx uses existing foreground tokens for header control/nav/incident text while preserving the incident accent; FinanceReviewQueueClient.tsx names/focus-enables all three tables. Actual native owner route loaded real25-dollar payment data and both canonical/admin paths with no failed requests. Four light/dark desktop/mobile Axe runs now have zero violations and no page overflow; mobile ArrowRight scrolling moved0→35/40. Existing26 component cases passed. Finance_source_inventory is independently reviewing these files, including whether the table outline is visually clipped by the overflow wrapper. Do not make another visual edit before persisting a new verdict; current .omx/state/hfa-native-header/ralph-progress.json is the second iteration at88 and needs final review.

HFA-NATIVE-UI first run failed only on tsx IPC paths:115-byte path versus local SDK sun_path[104]. Its exact failure artifact2026-09-09T13-41-56-002Z is retained; SQL345/26, build and routedAxe2 passed, while lint/stress/design hit IPC failures. Root started an unchanged gate retry using TMPDIR=runroot/u (83-byte IPC path); exec session50881 was confirmed live at dispatch. Revalidate that exact handle/results before deciding whether it finished. No budgets/assertions were relaxed. Native owner storage state is private at native-services/browser-owner/storage-state.private.json; preview4327, API59831/standaloneDeno59835. All11 additional browser SELECT grants matched verified hosted metadata and affect only the native fixture; RLS and financial write grants unchanged.

QBO read transport files remain untracked/uncommitted: _shared/qbo-read.ts/test.ts and F04-QBO-READ-CONTRACT.md. Initial24a1710c/72 tests received REQUEST CHANGES with six independently reproduced deadline/storage, timestamp, identity Content-Length, unknown report shape, malformed signal and abort-order findings. The author qbo_read_contract was found completed on its prior architecture task; root explicitly restarted repair with followup_task and the author acknowledged work. Use followup_task to start an idle/completed agent; send_message alone does not start it. Older agents cannot address this later sibling directly, so root relays source freeze/review messages. Reviewer evidence: runroot/qbo-read-review. No provider calls or activation.

Migration343 is reserved only to f00_independent_review and stays private in runroot/billing-run-343. Current prototype has167 assertions and complete2505 resident/payer/history/agreement/line capture plus600 invoices,26 pages and3,006,000,000-cent controls. Public wrappers bind expected entity and manifest/candidate hashes. Invoice/line outputs are excluded from the input mutex/revisions; manual/current conflicts are separate. Remaining before freeze: controlled FK/deadlock/after-wait authority tests, extra golden cases and clean replay. No app/Edge producer switch, scheduler principal or343 repository migration yet. Architecture is at runroot/billing-run-design/architecture.md. Root warned that old writers/implicit FKs can invert locks; deadlock/serialization must fully roll back, never become a duplicate/success.

The independent342 dynamic security lane remains blocked by an automatic possible-cybersecurity-risk filter; native F01 proofs do not replace it. Actual provider choices/company facts, controller/policy decisions, provider-controlled testing, recovery, rollout and named real cycles remain open. Continue implementable work; do not shrink the goal or treat a partial source segment as engineering completion.

### Latest live follow-up

HFA-NATIVE-UI retry completed PASS at test-results/agent-gates/2026-09-09T13-46-43-908Z-HFA-NATIVE-UI.json: lint,345/26 SQL replay, build, stress,8 design screenshots and2 authenticated routedAxe checks. Short ownedTMPDIR resolved the actual115-byte Unix socket path issue; localSDK sun_path is104 bytes. Keep first failure artifact.

Independent UI review found a remaining visible-focus defect despite Axe0: the table's outside outline was clipped by overflow. Root added focus-visible:-outline-offset-2 to the three named workbench tables after persisting visual verdict88/revise. Finance_source_inventory is actively rerunning visible-focus/scroll plus component tests; no UI approval or commit yet. CurrentAppShell andFinanceReviewQueueClient edits remain owned/uncommitted. The native UI fixture's production-matched read grants are documented; no RLS/financial-write change.

QBO author repair is now actually complete and frozen at sourceed72b25ef3a452cd870bcb2806b0583c54371432445cc893654e9a0655305faf/test9b51f9f4a6ab58fa369f2c75e34a5f8f7c2d211a87686d4855557bc3e6a6c6b1/doc720e50034b0d515448b66542a5ba2282f714844ff10b38a638f10c92fbbf347b.98 permanent tests and original repro8/11/2 pass. Root relayed freeze to finance_source_inventory for final independent review after UI. All six failed baselines remain in qbo-read-review. No QBO source committed yet.

343 author candidate stabilized at567d7f7b… with176 SQL assertions and19 controlled concurrency/lock checks; three final probes were still running when reported. Root assigned independent343 code/test review to qbo_read_contract, whose own QBO transport remains frozen. Native UI focused review is still with finance_source_inventory, followed by its QBO repaired-source re-review. Root owns only the two UI source files and shared records. No343 migration copied into repository yet.

Native UI final source now independently APPROVE: AppShellae9dec1f andFinanceReviewQueueClient0d640149. Inwardoutline repaired visiblefocus (1418ringpixels), fourmodeAxe0/nooverflow, actualkeyboardscroll and26componentcasesPASS. Visual95; evidence native-ui. Final focused checks follow the passed broadnativeUI gate; no broadaccessibility/hosted/business claim.
