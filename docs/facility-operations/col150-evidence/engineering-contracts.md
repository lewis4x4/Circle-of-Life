# COL-150 Needs Attention contract

## Live scope and prerequisites

COL-150 was read live September 12, 2026 and moved to In Progress. COL-149 and COL-144 dependencies are clear. Source starts from `c7859efa` in the isolated `codex/hfo-col150-needs-attention` worktree, stacked on PR #491. Current main remains `50bc07c4`, already integrated in that ancestry.

A company-wide green screen must not hide failed checks, missing evidence or failed reads. Aggregate overdue, unresolved issues, missing evidence, waiting, unassigned and configuration-needed states on the server over currently permitted facilities. Categories and denominators remain explicit; each result reaches matching detail/history/action. Counts reconcile to complete independent fixtures and filtered detail beyond existing caps. Later-page/source failure and unknown applicability never become zero or all-clear. A performed failed generator check retains its unresolved issue until separate resolution. No overall percentage hides severe issues.

## Boundaries

Q29 remains open: Operations/domain leads must decide immediate versus later urgency, recipient/backup, acknowledgment and escalation timing. This source delivery uses recorded states and approved due/follow-up instants; it adds no threshold, urgency score, notification channel or operating policy. Category overlap is explicit. Task status cannot close an issue.

All reads use session organization/site/subject authority; current authority is rechecked after waits. Missing or failed reads remain unknown. Exact detail is displayed in this new surface rather than linking a total to a capped legacy backlog. Existing receipt/evidence controls remain authoritative.

No production merge/deployment or human staff/clinical/operating acceptance is implied. Source/local verification, public preview UI checks and component axe remain distinct from authenticated hosted proof.

## Verification and rollback

Require behavioral fixtures beyond 1,000 records and a lower provider cap, later-page errors, unknown applicability, separate issue resolution, current-scope changes, detail/count reconciliation, UI stale responses and axe; then repository strict UI gate, typecheck and separate technical review. Results and any superseded attempts are retained.

Before release, revert only this bounded segment or close its stacked draft PR. Preserve predecessor integration and immutable evidence.

## Ownership read surface

Migration `364_hfo_attention_ownership.sql` adds a read-only, caller-guarded current-coverage projection. The public wrapper and view are invoker surfaces; the guarded private implementation reuses existing task readability and recipient eligibility checks. It does not expose the raw arbitrary-person helper or assign an owner. Task coverage follows the pinned named primary/approved backup; role-owned issues retain the existing issue-lifecycle semantics. Source migration only, not applied to hosted staging or production in this segment.

## Units and uncertainty

Categories overlap but each category counts a source record once. The unfiltered detail total is unique source keys, not a sum of category cards. Task and issue populations are explicitly named; waiting includes task verification and issue waiting. Missing requirements/applicability and source read failures cannot certify all-clear. An empty accessible-facility set is absent coverage, not a healthy company.

Pagination fingerprints the complete classified projection and its actor/facility/category scope; a changing projection demands a first-page reload. This is change detection, not a serializable export snapshot. The future export work retains its own cutoff contract.
