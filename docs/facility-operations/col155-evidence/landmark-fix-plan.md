# COL155 duplicate source-panel landmark fix

Actual authenticated1440/375 proof saved and rechecked reviews successfully, but axe reported landmark-unique when multiple source-context panels were expanded. Each panel's named section creates a same-named region landmark.

Replace only this unnecessary section landmark with a labelled div group inside the existing details/summary. Preserve forms, labels, payloads, source authorization and migration367. Add a two-simultaneously-open-panels axe regression, observe failure before the change, then require zero structural violations after. Retain initial hosted reports/screenshots and existing review receipts. Final browser proof will read/recheck these saved reviews rather than record duplicate performance; a reviewed source-transition binding is required before reusing the fixture under the new commit.
