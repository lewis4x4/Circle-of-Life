# COL155 guarded hosted proof preparation

Preparation only. No hosted calls, migration application, fixture creation or production mutation have run. Migration367 is not assumed applied. Port4355 proposed; source must be committed and reviewed, with clean mandatory gates before parent creates explicit readiness.

`guarded.py` reuses COL140's exact-source guard, including tracked and newly added runtime/runner files, private0600 stage config, fixed API/database/CLI targets, NoRedirect transport and fresh management identity. `apply-staging.py` accepts only the exact local migration version set with367 either absent or present. After an exclusive ledger lock it repeats exact count/version checks and applies only367+its ledger entry in one transaction. Existing367 must match the reviewed SQL text. No global push/reset or numeric timestamp-migration shortcuts.

The fixture runner uses the reviewed native probe schema and allocates a fresh isolated organization as well as a fresh run ID, actor, second actor, entity, two sites, resident, review tasks and source records; preserve IDs and checkpoints in a private0600 col155-only state file before mutation. Never reuse or reactivate any retired fixture, canonical resident/site or real rule. Publish only synthetic review rules inside the fresh organization using the canonical activity key and new activity UUID; never alter shared canonical organization drafts. UI uses the server-authorized activity key. Publish these isolated rules using the native approved mechanisms. Record these synthetic approvals as fixture data, never business approval.

Required ordered execution after readiness:

1. Verify source/gate/review hashes; freshly identify iwcnajanvjvynolltflw as Haven HFO Staging; inspect exact ledger. Apply reviewed367 atomically only if absent. Preserve every failed/uncertain attempt.
2. Prepare exact fresh site/resident/contact or vital and synthetic review occurrence. Check native table constraints and use final backend probe patterns. Authenticate both current owner/reviewer and an actor with HFO site authority but no native clinical visibility (first require actual200 workspace containing the exact HFO task, then native candidate denial;503 is never proof); use their actual issued JWT claims, no service-role candidate reads.
3. Launch owned4355 from exact source with explicit stage env. GET task-bound candidates over real HTTP; verify exact subject/period/version and minimal metadata. Cross-site/wrong-resident/dual-scope denial must not expose native details.
4. Record a current/self review through the actual source-review API using selected version and existing findings payload. Snapshot exact owned native rows before/after; compare bytewise to prove review command creates no care/order/dose/signature/arrival/contact/vital writes. Retry exact request and verify one immutable receipt/reference set. Reject stale/wrong versions and on-behalf/historical payloads.
5. Separately change only the synthetic native source as test setup, preserving native update authority/history. GET/recheck must show changed or ineligible state and retain original review history. Revoking native access must mask details; currentness failure alone must not erase still-readable original provenance.
6. Browser at1440 and375: actual selected site and task, period/family/selection, required findings, source-review save, evidence/verification separation and history/recheck. Full untruncated pageerror/console logs, axe and screenshots. No clinical creation controls or signed URLs. Reconcile screenshot scope and settled network evidence.
7. Retire only fresh actors/grants/sites/entity after all tests. Assert ban readback, inactive profiles, zero grants and exact site/entity retirement. Retain review/native/audit history. Stop only owned4355 listener/process tree and verify port closed. Record any retained fixture obligations explicitly.

The full36-item/41-component mapping stays visible. Only the fixed reviewed4-family subset is connected; unsupported components are explicit native/manual fallbacks. No provider signature,1823 clinical accuracy,DNR authority,arrival or full packet completeness is inferred.

## Reviewed runner refinements

Grant retirement updates revoked_at and preserves both subject/site grant rows; cleanup checks zero active grants and retained row counts. No canonical shared-organization rule inspection remains. Setup prepares separate fresh manual tasks for1440 and375 using the isolated synthetic rule. The browser identifies each exact task from an actual candidate response, selects the exact contact, submits findings through the real Record review button, checks its receipt/actor/version, then invokes Recheck and verifies appended check history. Page, console and HTTP failures are retained. Syntax/local guard tests only; these scenarios have not yet run against staging.
