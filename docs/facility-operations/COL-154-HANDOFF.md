# COL-154 — connect drill and generator records to the checklist first

**Implemented, independently reviewed and verified locally on the feature branch as source only.** The first two domain adapters are registered on the COL-147 source-link mechanism; no Homewood rule, day, time, count or deadline is set (Q06, Q09, Q14 stay open); no occurrence, schedule, reminder, UI, hosted migration, deployment, provider behaviour, policy activation or operating acceptance has occurred. Nothing is merged to `main`.

Worktree: `/Users/brianlewis/Circle of Life/Haven Facility Drill Generators`; branch `codex/hfo-col154-drill-generators`, stacked on COL-147 (`codex/hfo-col147-source-adapters`, `a625b75a`). Blockers COL-147 and COL-144 were verified Done as reviewed, gated, unmerged source; COL-154 was moved In Progress by the orchestrator before this work started. Model: Fable 5.1.

## Delivered

- Migration `347_hfo_drill_generator_sources.sql` (provisional stack-local slot; contract §0): `drill_log` gains an explicit finality lifecycle (`record_version`, `finalized_at/by`, `version_recorded_at/by`, `entry_reason`, `correction_reason`, `voided_at/by`, `void_reason`, typed `readings`, `outcome`, `issue_summary`) under a guard that keeps every existing and legacy-written row a **draft**, refuses content changes and soft deletes on a final row, hard deletes always, and every lifecycle write outside the commands; a new `asset_observations` table (generator test, carbon-monoxide check, extinguisher currency check against a named asset; `basis` constrained to `staff_observed`; no draft state; DML only through the commands; site-access RLS read); an immutable request ledger `operation_source_record_requests`; readers `haven.operation_source_read_drill_log` and `haven.operation_source_read_asset_observation` in the 346 shape (activity by catalog key, facility or asset subject, corrector as recorder, original finalization as the recording instant, drill instant in the facility timezone, conductor/observer as performer with on-behalf reason, readings as values, outcome and issue); session commands `record_asset_observation_review`, `correct_asset_observation_review`, `void_asset_observation_review`, `finalize_drill_log_review`, `correct_drill_log_review`, `void_drill_log_review` (shape → actor and site → lock → replay → state → domain write under a third token → 346 delivery under the caller's own authority → request row → reply); migration-only registration of `drill-log` (→ `hfo-al-m05-01`, `hfo-al-m06-01`) and `asset-observation` (→ `hfo-al-w01-01`, `hfo-al-w01-02`, `hfo-al-a07-03`) for the organisation 336 seeded, asserting the review activities are never allowlisted.
- Library `src/lib/operations/source-records.ts` (command bodies, list query, outcome guard, presenter, `currentRecordVersion`), COL-154 fragments and `current_record_version` in `receipts.ts`; routes `POST/GET /api/admin/operations/asset-observations`, `POST …/asset-observations/[id]` (correct | void), `POST …/drill-logs/[id]` (finalize | correct | void). No UI.
- Canonical contract: [drill and generator sources](../specs/27-facility-operations-drill-generator-sources.md). Settled engineering contracts: `col154-evidence/engineering-contracts.md`. The 346 probe's two allowlist assertions now exclude the two COL-154 adapters.

## Acceptance

1. A final approved drill/source log satisfies the correct site/activity/period once; separate review stays outstanding where required: probe (a legacy `drill_log` draft delivered through the generic route is `source_not_final`; finalizing it satisfies the fire-drill occurrence as `awaiting_verification` with the finalizer as recorder and a late entry reason, while the separate "Review fire drill coverage" occurrence stays pending and no rule can map a source to any review activity; an elopement drill satisfies only its activity; a finalizer outside the recorder list leaves the record final and the delivery `recorder_not_authorized` with attention; a tornado drill finalizes without a delivery; a drill in the previous period matches the older occurrence and a corrected date moves it as a fresh chain; a second drill of the same type in the week is a visible conflict) and the race script (finalize racing a person's recording of the same occurrence, both orders: the second is the conflict or the `Work is already recorded` refusal).
2. Two generators and source correction/void/replay behave correctly; a failed observation leaves the issue open: probe (a staff-observed generator test satisfies exactly its generator's occurrence once with the observer as performer and typed readings as values; replay by key returns the stored reply; the same key with other content conflicts; a second record for the same generator and week is a visible `conflict`, never a second satisfaction; a `fail` opens a `failed_result` issue bound to the receipt and leaves the occurrence `failed`; the void reverses the receipt, returns the occurrence to pending, leaves the `invalidated` attention row and the issue open; a correction of readings supersedes as a 344 chain; a stale expected version conflicts naming the current one; a correction that moves the record to the other generator invalidates the first occurrence and satisfies the second as a fresh chain, so one record never holds two effective receipts) and the race script (two records for one generator: the second waits on the occurrence and lands as a conflict; correction racing void, both orders: the second waits on the record lock and is answered by the first's state; the same key raced twice replays).
3. No automatic self-test or photo alone claims staff observation; unconfirmed Homewood day/time does not invent a deadline: probe (`automatic_self_test` and `photo_only` are refused by name and write nothing; a future instant, an unstated late instant, a failed outcome without an issue, a generator test against a non-generator, an extinguisher check against a generator, a retired asset, another site's asset, a stranger observer, malformed readings and unknown fields are refused and write nothing); no rule is published or confirmed at any site and no occurrence exists outside the probe's rolled-back fixture (the weekly Tuesday rule there is a fixture, not Homewood's schedule); `OWNER-DECISIONS.md` 3k records the eight engineering policies and the legacy drill consumers as an unresolved boundary.

Every guard is proven: direct DML on observations and the request ledger refused for session and service with and without the forged setting; lifecycle writes on `drill_log` refused outside the commands; the owner-run definer with the real token still cannot rewrite a voided or identity column, un-finalize, delete or silently restate; a forced audit-insert failure leaves no observation, request, delivery or receipt; the other site is denied and reads none of it; generic audit payloads of the ledger hidden; the public RPCs are invokers.

## Boundaries kept

No Homewood day, time, month, shift, count or deadline; no rule published; no reminder, scheduler, Edge Function, controller integration or external transmission; no change to the legacy drill consumers (220/221/250/125/200) beyond recording that they are legacy; no PHI (drills and assets carry none; residents are counted, never named); COL-143 hosted Storage untouched; the remaining domains are COL-155–159.

## Evidence

Focused suite and full suite, typecheck, lint, native replay with 30+ probes including `review_hfo_drill_generator_sources.sql` and the adjusted `review_hfo_source_links.sql`, the six-case race script with lock waits observed from `pg_stat_activity` (advisory and transaction-id), an independent adversarial SQL/runtime review with dispositions and re-verification, strict gate artifact: see [verification](col154-evidence/verification.json), [review](col154-evidence/independent-review.json) and [concurrency](col154-evidence/concurrency.txt). Local PostgreSQL uses Supabase stubs and synthetic fixtures (hosted default table privileges on `drill_log` are stubbed for the legacy-path assertions); hosted, provider, staff and operating acceptance are not established.

## Resume and rollback

Next in the roadmap: COL-155–159 (residents, employees, census, dietary, providers) register their adapters and readers by migration against the 346 contract with the same lifecycle discipline; COL-159 depends on this segment. Check live Linear for dependency readiness. Before deployment, rollback is reverting this segment. After application, drop `asset_observations`, `operation_source_record_requests`, the six commands, their wrappers, the readers, the helpers and the third token; delete the two adapter rows and five rule rows (allowlists are append-only by guard: retire them by migration instead if history must be kept); drop the `drill_log` lifecycle columns, constraints, index and guard trigger (existing rows are drafts, so no legacy row is changed by the migration itself). Mission alignment: **PASS** for the bounded domain connection; hosted and operating readiness: **RISK**.
