# COL-227 guarded R shortcut proof

Prepared only until parent readiness review. Reuses fresh management identity, mode0600 staging config, NoRedirect, exact local366 ledger and verified actor/entity/site retirement from the corrected billing proof runner. No invoice/business/schema mutation. Port4343 only.

Generate local-only before manifest with `python3 -B guard.py manifest --phase before`; parent readiness fields: target `iwcnajanvjvynolltflw`, exact checkout, appUrl `http://127.0.0.1:4343`, phase before/after, approved_by_parent true, scope `COL-227 synthetic fixture and browser proof`, source_manifest `{path,sha256}`. Runners and new runtime files are included; bind after separately only following reviewed one-line R-guard change.

After readiness: `python3 -B fixture.py setup --ready <path>`, `fixture.py serve --ready <path>`, `node browser-proof.mjs <path>`. Capture invoices and overview at375/1440. Native Playwright R must emit0 on invoices baseline,1 on overview baseline, and1 on both after. Visible Refresh button supplies a1-event positive control. Require an observed successful empty scoped invoice refetch for the button in every case and for R wherever its event is expected. A missing observation is a failed proof requiring endpoint/timing inspection, not a pass. The invoices R=0 baseline intentionally has no R refetch requirement.

Verify R keeps focused Refresh button/facility scope, `/` focuses the actual search input and E downloads read-only CSV. Type R into the actual search input. Textarea/select/contenteditable guards use clearly labeled synthetic DOM controls with native keystrokes, removed before axe/screenshots. Command/control cases use synthetic KeyboardEvents to test the application guard without causing browser-native reload; do not present them as browser shortcut tests. No F/G scope expansion.

Keep all before evidence; parent visual/review releases edit. After proof, retire exact fresh actor/entity/site with asserted ban/inactive/zero-grants readback and stop only owned4343. Keep source manifests private/untracked and use -B. Earlier COL221 R observations are context, not replacement for this fresh proof.
