# COL-151 backend verification attempts

- Initial direct `npx tsc --noEmit --pretty false` traversed tests and retained test-results Deno gateway files; pre-existing test typing and unsupported Deno imports reported. No export implementation errors reported. Repository-scoped `npm run typecheck` is the authoritative parent gate.
- Focused `npx vitest run src/lib/operations/history-export.test.ts src/app/api/admin/operations/activity-history/exports/route.test.ts --maxWorkers=2`: 2 files, 19 tests PASS. Exercises 1,201 tied rows, 73-row pages, correction chain preservation, actual CSV bytes/formula safety, explicit empty output, later-page interruption/retry, manifest mismatch/duplicate/truncation/count failures and download authorization revocation.
- Native PostgreSQL replay attempt 1: migration365 applied; new probe failed `correction chain incomplete` at line231 because the test assumed a tied-timestamp occurrence would be on its first500 rows. Fresh snapshot total and receipt total assertions already passed. Fixed test to examine all immutable pages, preserving timestamp/UUID order.
- Native PostgreSQL replay attempt 2: PASS, 368 migration files and 48 SQL probes. Probe creates 1,001 manual occurrences through actual commands plus 12 scheduled occurrences, checks full total and unique pagination, executes new occurrence and correction after snapshot creation, verifies old snapshot equality and fresh corrected totals, same-key replay/conflict, finalized evidence references without paths, requester/site/revoked/anonymous denial.
- After attempt2, expanded probe adds actual native document deletion/revoked-reference download denial, failed audit atomic rollback and immutable update/delete/truncate denial. These additions await parent strict gate; do not report earlier replay as verification of those new cases.

All SQL proof above uses the manifest-owned native PostgreSQL cluster with Supabase stubs, synthetic actors and rollback fixtures. It is not hosted Storage, browser, staff or operational acceptance.

- Two-session concurrency proof initial PASS, then expanded empty-export case PASS; reports retained in `concurrency-initial-result.json` and `concurrency-empty-result.json`.
- Corrected generation timestamp from PostgreSQL outer-statement time to `clock_timestamp()`: outer statement time includes time spent waiting before snapshot creation. `snapshot_id` remains the authoritative source boundary.
- Final two-session concurrency proof PASS against migration SHA-256 `46da090090b4a207ee29d6a92e4772e418102b3d7851fe05cdcc2cb98a95b277`; report `concurrency-result.json`. Observed actual lock waits before releasing another session: (1) concurrent correction and new occurrence commit while materialization waits, old snapshot stays12 occurrences/2 receipts and fresh snapshot has13/3; (2) facility grant revoked during request advisory wait returns42501 with no snapshot; (3) initially authorized empty export total0 is denied after facility revocation. Exact ephemeral database was dropped and connections closed.
- Parent reported an overlapping strict replay failure at `review_hfo_applicability.sql:5`, `tuple concurrently updated`: both processes altered cluster-global roles from different scratch databases. Serialized all subsequent native runs; final concurrency completed and cluster handed back idle. This is retained as infrastructure contention evidence; parent strict gate must be rerun alone.
