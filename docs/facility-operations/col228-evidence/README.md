# COL-228 overview heading prerequisite

Dedicated read-only proof using the still-active parent-owned COL227 fixture and4343 app. Do not create, reactivate or clean this shared fixture here; retain it until COL227 finishes. Only stop the exact owned app if parent needs the gate, preserving actor provenance for restart.

`before/` copies the unmodified COL227 baseline report/screenshots containing the actual overview heading-order violation. `before-reference.json` records hashes, source declaration and measurement limits. No isolated BEFORE heading rectangle/font was captured; do not invent one or claim a full pixel comparison from code alone.

After source-only h3→h2 change is reviewed, local `python3 -B guard.py manifest --phase after` creates the dedicated manifest. Parent readiness fields: target `iwcnajanvjvynolltflw`, checkout absolute, appUrl `http://127.0.0.1:4343`, phase after, approved_by_parent true, scope `COL-228 read-only shared-fixture browser proof`, source_manifest `{path,sha256}`, shared_fixture `{path:"/Users/brianlewis/.config/haven-staging/col227-fixture.json",owner_task:"COL-227"}`.

`node browser-proof.mjs <ready>` checks actual overview375/1440, logical heading hierarchy and zero axe violations; captures current computed font/rectangle and confirms unchanged text/classes against the recorded old source. It also rechecks unchanged R0 invoices/R1 overview and button1 at both widths. R handler remains untouched; these are additional baseline observations, not COL227 completion. No synthetic typing controls or business writes are used in this prerequisite proof.

Keep dedicated artifacts separate; COL227 runners/evidence remain untouched. No hosted execution before parent readiness.

Initial AFTER capture correction: parent visual review found All facilities selected at capture time and a different mobile scroll position. Those artifacts remain unchanged under `initial-after/` and are not valid fresh-site visual comparisons. The refined runner waits for the actual fresh-site invoice response and selected radio, completes scoped keyboard/refetch checks, then validates scope before and after each capture. Full viewport capture starts with window/main scroll at zero; the separate heading crop may scroll. Scope and scroll metadata are retained. This changes proof timing only; the h2 source and R handler stay unchanged.

When a gate has stopped the app, `python3 -B guard.py serve --ready <228-ready>` restarts4343 under the dedicated228 source guard and exact private staging environment, reusing only the explicitly active COL227 fixture. It performs no fixture creation or retirement.
