# COL-143 — in progress (source-only portion)

Worktree: `/Users/brianlewis/Circle of Life/Haven Facility Occurrences` (shared with COL-139, COL-142 and COL-144). Branch: `codex/hfo-col143-evidence`, base `89ed6aca` (COL-144 closure docs over `f97fb50f`).

Selected 2026-09-10 after COL-144 closed. Live Linear left two dependency-ready HFO issues, both externally blocked: COL-143 (this one; blockers COL-133 and COL-142 Done) and COL-140 (HFO-26, Homewood's approved profile, which needs the owner's answers to Q01, Q02, Q04–Q10, Q14 and Q30 and is not started). Every other HFO issue waits on COL-143, COL-145, COL-147, COL-148, COL-149 or COL-21.

**Precise blocker.** Acceptance item 3 requires an authorized administrator or assistant to perform a real upload, finalize, completion and corporate download with positive hosted Storage HTTP proof retained. Source-only work cannot establish hosted transport, so this issue stays In Progress, not Done, when the source segment is reviewed and gated; the hosted proof is a separate acceptance step for the owner or integrator, documented in the handoff. The other three acceptance items (wrong-site/subject/unowned/unfinalized objects cannot satisfy evidence and restricted files cannot leak through task metadata; required upload failure leaves performed/evidence-incomplete, optional failure never appears attached, retries deduplicate; a later valid retry satisfies the same performance once through appended evidence history with original attribution unchanged and safe concurrent correction) are source-verifiable and are built here.

Sources read: the live issue text; BUILD-SCOPE section 5 (Evidence row; "Evidence supplied after performance"); the source pointers (`src/app/api/admin/facilities/[facilityId]/documents/route.ts` upload pattern, `tasks/[id]/complete/route.ts` raw-path refusal, R03, R04); the most recent Storage pattern in `335_employee_file_lifecycle.sql` (private buckets with size and MIME limits, definer path-access function, restrictive `storage.objects` policies with no client update or delete, signed URLs from the session client); Q10 and Q23, which stay open.

Settled contracts: `col143-evidence/engineering-contracts.md`. Provisional migration slot 343, stacked on 342. Finance-first integration, fresh hosted-ledger read before final numbering, existing owner decisions and source-only release boundaries remain in force.
