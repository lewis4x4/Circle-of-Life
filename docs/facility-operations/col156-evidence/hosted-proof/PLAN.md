# COL156 real Employee File transport proof

Preparation only. Target fixed to iwcnajanvjvynolltflw / Haven HFO Staging, same dedicated staging CLI/PG/API identity. Port4356. Migration368 may be applied only atomically after exact committed runtime/runner hashes, independent clean review and mandatory gates bind to parent readiness. No production mutation.

Fixture: fresh isolated organization/entity/two sites; new employee identity, owner reviewer and manager without medical grant; staff row linked to employee; separate off-site staff. Publish only fresh synthetic TRN-24 personnel and DOC-039 medical requirements in the new site through existing Employee File requirement create/approve API. Required signers employee + administrator exercise genuine sign_record flow; reviewer must differ from employee. Grant medical access via native grant_medical command, plus HFO employee_medical scope separately. No existing draft/classification changes.

Transport sequence for BOTH categories: employee submits record; actual PDF bytes uploaded with current employee JWT to the correct private bucket under recordUUID/filename; attach_record; employee signs; independent owner signs administrator; native review_record verified; authorized download API creates audited signed URL, fetch it immediately with no redirect/target drift and compare SHA256 bytes. Never persist bearer URLs. Existing expiry and requirement classification govern. No provider signature claim.

Negatives: manager same-site personnel download succeeds but medical metadata/download denied; owner with native medical grant but missing HFO medical scope does not see medical HFO values; wrong-site employee file/download denied; revoke native medical grant via native API then reject new medical download and mask current HFO details; revoke site grant preserving rows and verify401/403/404. A previously minted signed URL may remain valid for its60secondTTL; do not claim immediate token cancellation. Test renewed request/current authenticated Storage access denial.

HFO: fresh canonical-key/newUUID activity+published synthetic rule and manual occurrence, no human performance creation. GET/explicit reconcile snapshot A, retry no append; reject or replace an actual native record to B then recheck once, retain A→B→A if returning to prior visible state. Exact audit/readiness no employment/duty/attendance mutations. Browser1440/375 real UI refresh/history with full axe/errors/scope proof. Medical data without scope stays unavailable, never aggregate clearance.

Lifecycle: persist exact IDs/checkpoints in private0600 col156-fixture.json before mutation; uncertain command stops for exact state inspection (native Employee File commands lack universal request idempotency). Preserve initial failures. Cleanup ban all ownactors and readback, deactivate staff/profiles, revoke native/HFO/site grants with history, retire site/entity/org. Keep employee records/signatures/audit and stored evidence retained as proof; no bucket deletion. Stop owned4356 and verify listener absent. Remove retired fixture session/password fields only after cleanup passes. No hosted calls before review.

Final denial order is explicit: manager personnel positive and medical denial; revoke only HFO medical scope, refresh current issued claims, confirm HFO medical redaction while native personnel AND medical downloads still succeed; revoke native medical grant, refresh claims, confirm new medical download denied while personnel remains readable; finally revoke site grant and confirm new personnel download denied. The no-grant actor's upload denial is not attributed exclusively to that missing grant because native ownership rules also apply. Actual denial HTTP statuses and zero-object readbacks are recorded. Storage evidence remains retained under retired fixtures.
