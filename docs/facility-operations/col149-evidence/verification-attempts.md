# Verification attempts

The first strict UI gate failed at the production build because Turbopack refuses node_modules symlinks outside its filesystem root. The isolated worktree now has its own dependency installation from the unchanged package lock; no dependency was added. The first full suite passed 4,421 tests with two skips before the final name/authority tests. An overlapping rerun was invalidated when the dependency symlink was replaced, causing module resolution failures; retain that failed run as superseded infrastructure evidence, not an application regression. Final authoritative outputs are `vitest-verified.log`, `typecheck-verified.log`, `segment-gates-final.log` and the latest timestamped strict gate artifact.

The 28 focused behavioral tests were independently rerun after the final functional changes. Authenticated hosted corporate-history browser acceptance has not been executed in this segment. The strict UI gate covers its configured public preview routes, while the new history page has component-level interaction and axe coverage. The existing hosted COL-143 evidence proves the dependency only. No visual reference image was supplied, so no screenshot-to-reference fidelity score is claimed.
