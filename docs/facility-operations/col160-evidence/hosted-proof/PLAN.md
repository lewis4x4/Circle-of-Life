# COL-160 guarded staging proof

Prepared only. Parent must freeze these runner bytes, commit, and supply exact readiness binding after clean required gates and independent source review. Importing scripts has no network or mutation. No runner has permission to infer readiness from an older source head.

1. `guarded.py manifest` records the complete runtime/config/assets/migration/runner file map inside the current private run. Parent readiness supplies target `iwcnajanvjvynolltflw`, exact checkout/head, localhost4360, manifest SHA, passing required PostgreSQL/UI segment gate and independent exact-head clean review. Every SQL mutation rechecks those identities and independently reads the dedicated Supabase project identity.
2. `apply-staging.py inspect/apply` accepts only the entire locally reviewed migration version set with371 missing or exactly present. It locks the ledger, rechecks370 and every version, and applies only371 plus its ledger in one transaction. Snapshot, command and history must preserve the reviewed current-authority-checked SECURITY DEFINER implementation, exactly empty fixed search_path and authenticated-only execute with PUBLIC/anon/service_role denied. This is verified for each function from the actual catalog; prior invoker assumptions do not describe371. No generic migration push.
3. `fixture.py setup` checkpoints synthetic Auth creation and one atomic native row transaction in a new random org, two sites, two residents and three actors. Four supported component rules have explicit synthetic applicability with schedule unknown; FPC has no invented task/subject. All side effects use exact owned IDs. Sessions are real hooked Auth sessions. Disposable fixture sessions/passwords and mutation checkpoints are created only at the private current-run `fixture-state.json` (mode0600). Parent records that exact path in its cleanup manifest and retires the owned fixture before deleting session state. Existing staging credential configuration, provider, resident, finance and other fixtures are untouched. `serve` requires exact port4360 absence and writes owned PID/cwd/head provenance before exec. No other listener may be stopped.
4. `http-proof.py exercise` uses real current-actor app HTTP and supported native Stand Up RPC. It proves stable requirement identity/replay/conflict, missing-site meeting preservation, owned missing follow-up, unknown due, no-change capture, census A-B-A, unchanged old meeting contents, prepared/sent/received/accepted separation/date precision, actual Stand Up revision, owned rejection, both trust components, contract preparation refusal, resident-isolated coverage/meeting history/cross-resident denial, and complete unequal event/version pagination. Pagination uses52 actual attributed configure commands, not fake private history inserts.
5. `browser-proof.mjs` runs actual Work UI at1440/375 with reduced motion, all reachable mappings and taskless FPC. It loads history pages, obtains actual server409 with an injected obsolete revision, and verifies definitive retry clearing. An actual200 preparation response is deliberately dropped after commit; the UI retries exactly identical bytes and returns the same capture/revision. This is controlled transport-fault injection over real staging writes, not a provider acknowledgment. Screenshots, axe, errors and overflow are retained. External browser origins are refused.
6. `http-proof.py deny` runs after browser proof, with positive scope control, HFO-only/native denial, permanent financial grant revocation and current actor deactivation. Protected history must disappear. No actor or grant is reactivated to make proof pass.
7. `fixture.py cleanup` bans all exact actors, revokes site/subject grants, deactivates profiles and retires exact sites/entity/org with fresh readback. Native synthetic source/history/audit is retained. Parent closes only the owned4360 process tree and records listener absence, independent hosted review and source manifest equality.

No production, provider/email, Front Office receiver request, live schedule, operating acceptance, staff acceptance or human approval is performed or claimed. Local source/parser proof remains separately documented.
