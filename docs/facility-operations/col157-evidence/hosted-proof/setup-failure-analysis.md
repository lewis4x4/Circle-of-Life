# COL157 fixture setup failure and bounded repair

Hosted setup on source1dacb390 failed during the atomic `rows` transaction. Readback shows organization0,profile0,site0; two exact fresh Auth users exist and private pending=rows remains. No financial/census/native fixture rows committed. The hosted wrapper suppressed SQL detail; the source of the failure was reproduced separately in an owned local369 database clone, not asserted from an unavailable hosted message.

The exact reconstructed local transaction raises `Sign in required` from migration320 `haven_apply_cash_transaction`: the trust transaction was inserted before owner login. The repair moves only that insertion to an explicit `trust_recorded` checkpoint after fresh real owner login, using existing sql_user with actual issued hooked claims and SETLOCALauthenticated. Account initialbalance0 and legacyrow setup remain unchanged; transaction native UUID and historical occurred_at remain exact. This is authenticated native SQL fixture setup, not an HTTP cash command; `post_cash_transaction` does not accept historical occurred_at, so no economic date is changed to fit it.

Final local proof: local-fixture-corrected-stub-auth.log passed with balance500 and rollback. Earlier corrected runs encountered local baseline omissions (default trust table grants and auth schema usage), retained in local-fixture-corrected.log and local-fixture-corrected-native-grants.log. Final local-only auth.uid/schema/default-grant shims match existing native probes; they are not introduced to staging or runtime. The actual hosted repair uses real issued sessions, not generated claims or an auth.uid replacement.

No source/API/migration369 change. Hosted retry requires independent review, runner-only commit, exact source/readiness binding, audited original private-state preservation and explicit reset of pending rows only after confirmed zero-row rollback. No new Auth users, grant restoration or blind retry. The exact local clone and SQL artifacts are recorded under the current run's private scratch provenance.
