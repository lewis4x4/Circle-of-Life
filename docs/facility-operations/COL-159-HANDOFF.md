# COL-159 — connect dietary, facility services and general admin evidence

**Implemented, independently reviewed and verified locally on the feature branch as source only.** The AL coverage pass for the twenty-one COL-159 items is complete: every item is a registered source path on the COL-147 mechanism, a human recording path with its question named, or a review activity that is never allowlisted; no Homewood rule, day, time, threshold, reading definition, equipment list or deadline is set (Q09, Q11, Q14, Q28 stay open); nothing is sent or published; no occurrence, schedule, binding, applicability, reminder, UI, hosted migration, deployment, provider behaviour, policy activation or operating acceptance has occurred. Nothing is merged to `main`.

Worktree: `/Users/brianlewis/Circle of Life/Haven Facility Dietary Admin`; branch `codex/hfo-col159-dietary-admin`, stacked on COL-154 (`codex/hfo-col154-drill-generators`, `47455a13`). Blockers COL-147 and COL-154 were verified Done as reviewed, gated, unmerged source; COL-159 was moved In Progress by the orchestrator before this work started. Model: Fable 5.1. Segment id: `COL-159-HFO-DIETARY-ADMIN`.

## Delivered

- Migration `348_hfo_dietary_admin_sources.sql` (provisional stack-local slot; contract §0 — `origin/main` now also carries a `336_stand_up_pilot.sql`, so the whole 336–348 run renumbers at integration): `asset_observations` widened by `aed_operation_check` and `aed_equipment_check` (record/correct commands and reader replaced in place); new `facility_service_records` (site kinds fire safety, fire and sprinkler inspection; asset kinds extinguisher inspection, hood cleaning and AC filter change; staff or site-linked vendor performer; read-only `next_due_on` and certificate reference; final by command only, versioned corrections, void with a reason, site-access RLS read, audit, no delete); new `dietary_records` (meal-level substitution with service date and meal period and no resident, dietitian menu approval with labels verbatim, emergency food supply check); readers `operation_source_read_facility_service`, `operation_source_read_asset_service`, `operation_source_read_dietary_record`; commands `record/correct/void_facility_service_review` and `record/correct/void_dietary_record_review` (shape → actor and site → request-key lock → record lock → replay → state → domain write under the token → 346 delivery → request row → reply); migration-only registration of `facility-service`, `asset-service` and `dietary-record` with eleven rules, asserting the five COL-154 rules untouched and no review or human-path activity allowlisted.
- Library `src/lib/operations/source-records.ts` (service and dietary bodies with composite refinements, list queries, selects; AED kinds), COL-159 trusted fragments in `receipts.ts`; routes `POST/GET /api/admin/operations/service-records`, `POST …/service-records/[id]` (correct | void), `POST/GET /api/admin/operations/dietary-records`, `POST …/dietary-records/[id]`. No UI.
- Canonical contract: [dietary and facility-services sources](../specs/27-facility-operations-dietary-admin-sources.md). Settled engineering contracts and the coverage register: `col159-evidence/engineering-contracts.md`. The COL-154 and COL-147 probes' registration assertions are widened to exclude the COL-159 rows.

## Acceptance

1. Every remaining item has an accessible capture/source path or a specifically identified question; no silent omissions: the coverage register (contract §1) dispositions all twenty-one items; the migration asserts the eleven new rules exactly, the COL-154 rules untouched and no review or human-path activity allowlisted; the probe asserts the same and that site B's record lands `unmatched` and visible.
2. Meal-level records and audit/performance are distinct; relevant equipment/site applicability controls tasks: probe (a substitution satisfies exactly its period once and a second in the period is a visible conflict; a substitution not on its service date, a meal service of another date or period, and a failed substitution or approval are refused by name; the review activities are never allowlisted; asset kinds refuse the wrong asset type, a retired asset and another site's asset by name; the second extinguisher's occurrence stays pending until its own record) and the race script (two AED checks for one AED and week: the second waits and lands as a conflict; the equipment component untouched).
3. Composite checks preserve evidence components; service recording does not silently advance unrelated lifecycle; outbound content not auto-published: probe (AED operation and equipment are two records; fire and sprinkler from one visit are two records; an extinguisher inspection leaves the currency check pending; the assets including the inspected extinguisher's service dates and vendor, the building profile, the facility, the documents, the meal service and the ticket counts are snapshotted before and asserted unchanged after every record; a failed food check opens an issue its void leaves open) and the contract (social media, company calendar and activity calendar stay on the human path; the reputation posting routes are not connected). The race script proves a service record racing a person's recording, both orders, and a correction racing a void, both orders, with observed lock waits; the same key twice replays.

Every guard is proven: direct DML on both tables refused for session and service with and without the forged setting; the owner definer with the real token cannot rewrite identity, kind, a voided row, delete, silently restate or move an asset off-site; a forced audit failure leaves nothing; the other site is denied and reads none of it; a site service delivered through the asset adapter is a recorded `reader_failed`; generic audit payloads of the ledger hidden; the public RPCs are invokers.

## Boundaries kept

No Homewood day, time, month, threshold, reading definition, equipment list or deadline; no rule published; no binding or applicability; no reminder, scheduler, Edge Function, integration or external transmission; nothing sent or published; no vendor created from a name; no PHI (no resident is referenced; per-resident dietary data stays on the 237 tables); the legacy `ac_filter` maintenance consumer untouched; COL-143 hosted Storage untouched. `OWNER-DECISIONS.md` 3l records the twelve engineering policies.

## Evidence

Focused suite and full suite, typecheck, lint, native replay including `review_hfo_dietary_admin_sources.sql` and the widened COL-154/COL-147 probes, the six-case race script with lock waits observed from `pg_stat_activity` (advisory and transaction-id), an independent adversarial SQL/runtime review with dispositions and re-verification, strict gate artifact: see [verification](col159-evidence/verification.json), [review](col159-evidence/independent-review.json) and [concurrency](col159-evidence/concurrency.txt). Local PostgreSQL uses Supabase stubs and synthetic fixtures; hosted, provider, staff and operating acceptance are not established.

## Resume and rollback

Next in the roadmap: COL-155, COL-156, COL-157 and COL-158 register their adapters against the same contract; COL-161 proves the core on real surfaces. Check live Linear for dependency readiness. Before deployment, rollback is reverting this segment. After application, drop `facility_service_records`, `dietary_records`, the six commands, wrappers, three readers and helpers; restore the 347 bodies of the observation record/correct commands and reader and the three-kind CHECK; delete the three adapter rows and eleven rule rows (or retire them by migration if history must be kept). Mission alignment: **PASS** for the bounded domain connection with every item dispositioned and nothing invented; hosted and operating readiness: **RISK**.
