# Facility Operations — decisions pending the owner or integrator

Updated 2026-09-10. These are the questions the overnight work could not and did not answer. Each names the owner, the evidence, and what happens until it is decided. Nothing below has been guessed in code.

## Owner decisions

1. **Named individuals in source header labels (COL-132).** `activity-catalog.json` items AL-Y02 ("Fire Safety Inspection - Chad Croft") and AL-C08 ("Michelle Norris sent copy of contract") reproduce workbook header text naming people; the seed writes them permanently into `operation_activity_source_items.source_payload`. Options: redact the `sourceText` to a neutral label with an entry note before deployment, or accept workbook header wording as evidence. Until decided: the branch keeps the workbook text and the spec says so; do not deploy migration 336 without the ruling. Evidence: `col132-evidence/review-closure.json`.
2. **Whole-run fail-closed automation on population mismatch (COL-133).** The risk scorer and staffing computer stop the entire service run when one organisation's classified projection disagrees with its raw population. The reviewer suggested per-organisation continuation. The current behaviour is the conservative choice (partial coverage is never labelled complete); a per-organisation `continue` with deterministic ordering is a cheap change if preferred. Evidence: `col133-evidence/integration-review.json`.
3. **Open configuration questions Q01, Q02, Q07, Q08, Q10, Q11 (all HFO issues).** No answer was assumed. `needs_confirmation` remains the published state for every unanswered item. Answers are required only before the dependent rule or activation, not before foundation build.
3a. **Schedule questions Q04, Q05, Q06, Q07, Q08, Q09, Q14 (COL-137).** The evaluator can express each candidate meaning (weekday sets with or without holidays, business-day ordinals against a versioned calendar, six-month interval versus fixed half-year, two-year interval, expiry anchors, event timing) so an answer can be recorded as a rule without code changes. It refuses to pick one: no site holds a rule, `bi-annual` is not a recurrence kind, and the seven ambiguous source items (AL-M05, AL-M06, AL-A07, AL-Y05, AL-Y07, AL-E14, AL-H01) carry no approved rule. Evidence: `col137-evidence/`.
3b. **Evaluator engineering policies to confirm or change (COL-137).** Three deterministic choices are tested and reported, not hidden; any of them is a small change if the owner prefers otherwise. (i) A local deadline time that does not exist on a spring-forward day is shifted forward past the gap, and a repeated fall-back time resolves to its first instant; both are reported on the occurrence. (ii) Holiday calendars are embedded and versioned inside the rule that uses them (immutable once published) rather than held in a shared calendar table; a shared registry can come with HFO-26 if the same calendar must be maintained in one place. (iii) Existing legacy templates keep their recorded timing through the translation (shift base time plus the first enabled escalation SLA, or the estimate floor of 60 minutes, as the deadline offset); this preserves the current scheduler output until an approved rule replaces it and is not an endorsement of using an escalation SLA as a deadline.
3c. **Occurrence engineering policies to confirm or change (COL-139).** Six deterministic choices are tested and reported, not hidden; each is a small change if preferred. (i) A facility-kind activity needs no explicit binding: the applicable, confirmed site configuration enrols the site itself, and the generator resolves or creates the site's one facility subject; residents, employees and assets are generated only from an explicit effective-dated binding made by a site administrator who holds that subject's authority. (ii) When a bound subject stops being current (asset retired, resident transferred, employee terminated), the service reconciliation closes the binding and cancels only pending occurrences whose period has not started; occurrences in a current or past period stay as they are for a human decision. (iii) Managed occurrences refuse the legacy defer and reinstate commands because those create replacement rows without identity; a scoped defer for managed rows is not designed here. (iv) Manual unscheduled work requires a central version in force and a site configuration in force whose applicability is not `not_applicable`; a `needs_confirmation` schedule is expected. (v) An association of early, late or unknown-schedule work to a scheduled occurrence is one-to-one and records nothing about completion; whether an association satisfies the occurrence is COL-142's decision. (vi) A binding may name a shift and the generation command accepts shifted evaluator items, but the version-1 rule shape carries no shift, so the scheduler never sends one and a shifted binding reports `no_binding`; shift-scoped generation waits for a shifted rule shape rather than inventing a shift from the deadline time. Evidence: `col139-evidence/`.
3d. **Receipt engineering policies to confirm or change (COL-142).** Seven deterministic choices are tested and reported, not hidden; each is a small change if preferred. (i) A performed time later than the server clock plus two minutes is refused; a performed time earlier than fifteen minutes before recording requires an explicit `late` entry with a reason. (ii) Until COL-143 supplies verified evidence, every required evidence rule that applies to the outcome is unmet, so an evidence-requiring activity records as `performed_missing_evidence` and its occurrence stays in progress; nothing reports it complete. (iii) Outcomes are `performed`, `failed` (an issue is created with the receipt) and `not_performed` (a reason is required); Q10's question of what allows an item to count as done is not answered by these values. (iv) Only `completed` changes the occurrence status; `awaiting_verification`, `performed_missing_evidence`, `failed` and `not_performed` keep it `in_progress` with the state visible, rather than adding new status values to the legacy check. (v) `completed_at` is the server instant of the receipt that completed the occurrence and `performed_at` is the stated work instant; `sla_met` compares the performed instant with the grace end or deadline. (vi) An occurrence that carries recorded work cannot be cancelled and a cancelled occurrence cannot be verified; changing recorded work is a correction (HFO-08). (vii) For managed occurrences the published recorder list (the site's local list when set, else the central list) decides who may record; the legacy queue hint `assigned_role` no longer gates recording on managed rows, because the order of a published role list is not a rule anyone wrote down. Evidence: `col142-evidence/`.
3e. **Issue lifecycle engineering policies to confirm or change (COL-144).** Seven deterministic choices are tested and reported, not hidden; each is a small change if preferred. (i) Assigning, reassigning and reopening an issue is a manager's act (the COL-133 broad operations list); the reporter may also reopen; the owner and backup may wait, resume and resolve. (ii) A backup may take over only when the owner is no longer current at the site (inactive, deleted, grant revoked or expired) or with an explicit cover reason; the takeover is recorded as `covered`, never as the owner's own acceptance. (iii) Waiting requires a reason and a future follow-up instant; the backlog flags an overdue follow-up but nothing is notified (Q29 open; HFO-15 owns reminders). (iv) Reopening clears the resolution columns on the row and keeps every resolution verbatim in the event history; the reopen count is visible. (v) Ownership is never inferred from a role name or a duty split (Q02 open): an issue is unassigned until someone names an owner, and the backlog shows it as such. (vi) An occurrence that still carries an unresolved issue cannot be cancelled or removed, so open work never leaves the backlog through a cancellation; an issue whose native subject stops being current (asset retired, resident transferred, employee terminated) hides with the subject under the COL-133 rule and is not resolvable until a historical-record scope exists, which is the COL-133 boundary rather than a new choice; likewise a resolved issue whose occurrence is later removed becomes unreadable with the occurrence while its rows and events stay in the tables. (vii) Owners and backups must hold an operations role (the list site access enforces); a person without one can be neither, even with a site grant. Evidence: `col144-evidence/`.
3f. **Evidence engineering policies to confirm or change (COL-143, source portion).** Five deterministic choices are tested and reported, not hidden; each is a small change if preferred. (i) One private bucket `operation-evidence` with four allowed types (PDF, JPEG, PNG, WebP) and a twenty-mebibyte limit; the evidence id owns the object path. (ii) Checksum verification is in source (amendment `col143-evidence/checksum-contracts.md`, 2026-09-10): the client declares the MD5 of the bytes it uploads and the database verifies it against the Storage service's eTag, the one content fingerprint the client did not write, when the upload is marked and again at finalization; a mismatch or a changed object fails the row durably (`checksum_mismatch` / `object_changed`, no exception, both values on the event), and an eTag that is not a plain MD5 leaves the row unverified and it never finalizes. An optional SHA-256 stays informational. The hosted proof gains one confirmation step (COL-143-HANDOFF, step 4): confirm the hosted eTag of a signed-URL upload is the plain MD5 of the bytes and that `owner` is the uploader; if it is not, nothing finalizes until the recorded fallback (an app-tier verifier streaming the object through the session's signed URL under a database-held secret) is proven. (iii) Only the uploader may mark, finalize or fail an in-flight row, and only the uploader can see it before it is finalized; an abandoned in-flight row attaches to nothing and blocks nothing, and a fresh preparation by any recorder proceeds. Supplementary evidence (no rule label) never counts toward satisfaction and never blocks it. (iv) When later evidence completes a performance, the occurrence completes at the finalization instant with the original performed-at unchanged; the finalizer is recorded on the evidence event, and the receipt's completion mirrors are written as the recorder's own completion (no second performer is invented). (v) `reading` evidence rules stay unmet until a later issue satisfies them from receipt values. Evidence: `col143-evidence/`.
3g. **Correction engineering policies to confirm or change (COL-145).** Seven deterministic choices are tested and reported, not hidden; each is a small change if preferred. (i) Anyone on the governing recorder list with current site and subject authority may correct or reverse recorded work; no separate manager rule exists (Q29 open). (ii) A correction is a full restatement of the work with a reason, not a patch; a correction that omits the performed time keeps the instant it corrects. (iii) The late rule for a correction is anchored on the original recording instant of the chain, and a corrected performed time can never follow that recording. (iv) Finalized evidence carries across a correction chain and never across a reversal; an upload in flight during a correction stays on the superseded receipt and must be re-prepared. (v) A correction always reopens review when the rule requires review, superseding the earlier review; independence is judged against the effective receipt. (vi) A reversal returns the occurrence to `pending` or `missed` by its deadline; cancellation after a reversal is allowed; an issue created with a receipt that is later superseded or reversed stays open and bound to that receipt. (vii) Legacy writers (complete, defer, reinstate, escalate, bulk, meeting synchronisation, service or session updates) refuse managed rows rather than being translated into receipts; the ordinary start transition and the scheduler's legacy-only insert are retained as COL-142 and COL-139 decided. Evidence: `col145-evidence/`.
3h. **Recovery engineering policies to confirm or change (COL-146).** Four deterministic choices are tested and reported, not hidden; each is a small change if preferred. (i) Drafts live server-side under the actor who saved them and expire after twenty-four hours; reconcile marks expiry on touch and resume or discard of an expired draft refuse. (ii) A resume replays the stored arguments through the idempotent command and never edited content; a resume of an already reconciled draft replays the one record. (iii) The same person may reconcile and resume from a new session; nobody else may see, resume or discard a draft, and a revoked or expired grant hides the actor's own drafts. (iv) The browser keeps nothing beyond memory (no local, session or indexed storage and no cookies), so a shared device carries no payload; the paper downtime process stays the outage fallback (Q25 open). Evidence: `col146-evidence/`.
3i. **Staff workspace engineering policies (COL-148).** (i) Upcoming covers fourteen days after the facility’s today and excludes finished work. (ii) History uses fifty-row keyset pages ordered by due time (nulls last), then ID descending; its separately counted total is site-wide even when Mine narrows a page. (iii) Legacy tasks retain their existing commands in a separate group, including all earlier unfinished work. (iv) One-action Complete applies only when the published rules require no other inputs or evidence for a performed outcome. (v) Provider-capped reads are paginated and ID lookups batched; partial reads never masquerade as complete lists. (vi) Evidence summary reports required rule count and aggregate satisfaction, not a file count. (vii) A single-site selector and fixed view controls replace the shared FilterBar’s multiple-site and arbitrary-date controls. These are source engineering choices; Q10, COL-140 policy approval and staff/device acceptance remain open. Evidence: `col148-evidence/`.

3j. **Source-link engineering policies to confirm or change (COL-147).** Seven deterministic choices are tested and reported, not hidden; each is a small change if preferred. (i) The source record's author is the receipt's recorder and must be an active person currently holding a role on the activity's published recorder list; a delivery whose author is outside that list or deactivated is refused with attention rather than satisfying silently or being hidden. (ii) The late rule (fifteen minutes) is anchored on the source's own recording instant, so a record entered long after the work must state `late` with a reason in its own statement. (iii) A corrected source version supersedes the earlier source receipt as a 344 correction and reopens review when the rule requires it; a version delivered after a person corrected the source receipt is a conflict, never an overwrite. (iv) A void reverses only a receipt this source wrote and that is still effective, always leaves an `invalidated` attention row that only a dismiss with a reason closes, and returns the occurrence to `pending` or `missed` by its deadline. (v) A delivery that finds a person's receipt on its occurrence is a conflict for reconciliation; the person's receipt stays effective. (vi) `select` resolves ambiguity only among the predicate's current candidates; reconciliation never overrides the period, and the COL-133 broad operations scope (owner, org_admin, facility_admin, manager, admin_assistant, coordinator) is the reconcile authority. (vii) A session may deliver only for a site it currently holds and, for a protected subject kind, only with that domain's recording scope, and sees only replies and ledger rows it could read; a record is the effective receipt of at most one occurrence, so a later version whose date moves to another period invalidates the earlier receipt first. No adapter, reader, rule or source is registered; the allowlists are migration-only. Evidence: `col147-evidence/`.

## Integrator decisions (before merge, apply or deploy)

4. **Listing completed audit export jobs.** COL-133 hides completed export jobs from the requester's list; Finance's UI expects to list snapshot-bearing completed jobs for download. Retrieval stays authority-checked in the database, so allowing the list to show completed jobs that carry a snapshot would lose no protection. Decide at integration; the reconcile migration does not change the list policy. Evidence: `col133-evidence/finance-integration/resolution.json`.
5. **Merge order and migration numbers.** Finance's export-job column type change must apply before COL-133's export-job policies (PostgreSQL refuses ALTER TYPE on a column referenced by a policy; reproduced). Rehearsed order: Finance 336–342, then COL-132 catalog, COL-133 authority, COL-135 applicability, COL-137 evaluator (339, replaces two 338 functions in place so it must follow 338), COL-139 occurrences (340, replaces two 337 function bodies in place and widens the 199 audit event check, so it must follow 339), then the audit-export reconcile. Re-read the Haven hosted ledger (project manfqmasfqppukpobpld; numbered through 335 on both 2026-09-10 reads, 344 records with 9 timestamp-style entries) immediately before assigning numbers, and update the seed script's hard-coded catalog migration filename in the same commit.
6. **Production dry-run of the catalog backfill (COL-132).** The 336 backfill aborts on cyclic or cross-site template lineage and writes audit rows for every existing template and instance. Run the dry-run against a current production snapshot before apply.

## Not decisions, just boundaries

- No hosted migration, deployment, schedule activation, external transmission or access grant happened overnight or during the COL-137 and COL-139 segments. Draft PRs #465 (COL-132), #466 (COL-133), #467 (COL-135) and #468 (COL-137) exist for review only; COL-139 adds its own draft PR against the COL-137 branch. No binding enrols any real resident, employee or asset; the only bindings that exist are synthetic probe fixtures that roll back.
- Local PostgreSQL replays use Supabase stubs and synthetic fixtures; hosted Auth, Storage, browser and staff acceptance remain separate gates.
