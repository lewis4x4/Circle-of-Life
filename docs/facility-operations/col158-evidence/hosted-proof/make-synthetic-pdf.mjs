// Local-only readable synthetic report. Explicit current-run scratch output; no network.
import fs from 'node:fs';
import path from 'node:path';
import os from 'node:os';
import {createRequire} from 'node:module';
const root=process.cwd(),out=process.argv[2];
const scratch=path.join(os.homedir(),'.hermes/tmp/agent-runs/haven-overnight-20260912-01a09869');
if(!out||!path.resolve(out).startsWith(scratch+path.sep)||fs.existsSync(out))throw Error('Fresh explicit current-run scratch PDF output required');
const factsPath=process.argv[3];
const facts=factsPath?JSON.parse(fs.readFileSync(factsPath)):null;
if(facts&&(!['today','due_on','effective_date'].every(key=>/^\d{4}-\d{2}-\d{2}$/.test(facts[key]))||!['One','Two'].includes(facts.version)))throw Error('Invalid synthetic facts');
const require=createRequire(path.join(root,'package.json'));const {chromium}=require('playwright');const browser=await chromium.launch({headless:true});
try{const page=await browser.newPage();await page.route('**/*',route=>route.abort());await page.setContent(`<html><head><style>@page{size:Letter;margin:0.7in}body{font:16px Arial;line-height:1.5}.page{break-after:page}.signature{border-bottom:1px solid #222;font:italic 24px Georgia;margin:24px 0 8px}</style></head><body><section class="page"><h1>SYNTHETIC TEST REPORT — NO REAL PERSON</h1><h2>Support Plan — Version ${facts?.version??'One'}</h2><p>This readable fixture tests native resident document transport and operator-observed signature citations. It is not a clinical plan, provider attestation, or approval of care.</p><p>Service: synthetic test interaction. ${facts?'Cadence, renewal and required signer set: unknown. The dated approval below is synthetic test evidence only.':'Cadence, report deadline, renewal and required signer set: unknown.'}</p>${facts?`<p>Synthetic documented due approval: Synthetic Owner records approval on ${facts.today}, effective ${facts.effective_date}, for report due on ${facts.due_on}. This statement applies only to this isolated software test, not a real provider or operating policy.</p>`:''}<p>Page 1 of 2. The test role marks on page 2 are synthetic evidence, not signatures of actual people.</p></section><section><h1>SYNTHETIC SIGNATURE EVIDENCE PAGE</h1><p>Page 2 of 2. These example marks are intentionally synthetic. Operators may cite this page as observed documentary evidence only.</p><div class="signature">Synthetic Caseworker Example</div><p>Role shown: caseworker. Signed date: not provided.</p><div class="signature">Synthetic Resident Example</div><p>Role shown: resident. Signed date: not provided.</p><div class="signature">Synthetic Administrator Example</div><p>Role shown: administrator. Signed date: not provided.</p><p>No verified electronic signing, actual provider service, document renewal or required signature policy is established.</p></section></body></html>`);await page.pdf({path:out,printBackground:true});fs.chmodSync(out,0o600);console.log('Readable synthetic two-page PDF generated at explicit private path');}finally{await browser.close();}
