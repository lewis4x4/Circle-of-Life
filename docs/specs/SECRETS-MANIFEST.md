# Secrets Manifest

> **Drift prevention:** Use exact backticked secret names in every row and keep `.env.example`, `AGENTS.md`, `supabase/functions/README.md`, and `docs/specs/PHASE1-OPS-VERIFICATION-RUNBOOK.md` aligned with this manifest. Add or update CI checks so required rows remain synchronized.

Canonical source of truth for environment variables and secrets used by Circle of Life runtime surfaces.

| Secret | Runtime | Consumer(s) | Required where | Auth header / usage | Rotation cadence | Rotation owner | Verification command | Referenced by |
|---|---|---|---|---|---|---|---|---|
| `EXEC_KPI_SNAPSHOT_SECRET` | Supabase Edge, Next.js server, Netlify, pg_cron/Vault | `exec-kpi-snapshot`, `/api/admin/executive/refresh`, executive refresh pipeline cron | Supabase Edge secrets, Netlify env, Supabase Vault | `x-cron-secret` | Quarterly or on leak | Brian Lewis | `supabase secrets list` | `.env.example`; `AGENTS.md`; `supabase/functions/README.md`; `docs/specs/PHASE1-OPS-VERIFICATION-RUNBOOK.md` |
| `RESIDENT_SAFETY_SCORER_SECRET` | Supabase Edge, Next.js server, Netlify, pg_cron/Vault | `resident-safety-scorer`, `/api/admin/executive/refresh`, executive refresh pipeline cron | Supabase Edge secrets, Netlify env, Supabase Vault | `x-cron-secret` | Quarterly or on leak | Brian Lewis | `supabase secrets list` | `.env.example`; `AGENTS.md`; `supabase/functions/README.md`; `docs/specs/PHASE1-OPS-VERIFICATION-RUNBOOK.md` |
| `RISK_NIGHTLY_SCORER_SECRET` | Supabase Edge, Next.js server, Netlify, pg_cron/Vault | `risk-nightly-scorer`, `/api/admin/executive/refresh`, executive refresh pipeline cron | Supabase Edge secrets, Netlify env, Supabase Vault | `x-cron-secret` | Quarterly or on leak | Brian Lewis | `supabase secrets list` | `.env.example`; `AGENTS.md`; `supabase/functions/README.md`; `docs/specs/PHASE1-OPS-VERIFICATION-RUNBOOK.md` |
| `DAILY_CENSUS_LOG_SECRET` | Supabase Edge, pg_cron/Vault | `daily-census-log` (COL-414 daily census writer) | Supabase Edge secrets, Supabase Vault (`daily_census_log_cron_secret`) | `x-cron-secret` | Quarterly or on leak | Brian Lewis | `supabase secrets list` | `.env.example`; `supabase/functions/README.md`; `scripts/census/cron-schedules.sql`; `scripts/scheduled-jobs/monitor.mjs` |
| `OFFICER_GATEWAY_HMAC_FRONT_OFFICE_V1` | Supabase Edge | `officer-catalog` (Front Office capability federation, key `front_office_v1`) | Supabase Edge secrets only (the same value lives in Front Office as `FRONT_OFFICE_TARGET_HMAC_HAVEN`) | HMAC-SHA256 over `x-fo-*` headers + raw body; the database holds only this name in `officer.gateway_keys.secret_env` | Quarterly, on leak, or on Front Office rotation (add a new key row + secret, then disable the old key) | Brian Lewis | `supabase secrets list --project-ref manfqmasfqppukpobpld` shows the name; a signed `{"op":"catalog"}` returns 200 | `docs/specs/OFFICER-CAPABILITY-CATALOG.md`; `supabase/functions/README.md`; `.env.example` |
| `SUPABASE_ACCESS_TOKEN` | GitHub Actions | `.github/workflows/edge-functions-deploy.yml` | GitHub repository secrets | Supabase CLI auth token for deploy/list | Quarterly or on leak | Brian Lewis | GitHub repo settings → Secrets and variables → Actions | `docs/designs/2026-05-24-hygiene-pipeline-plan.md` |
| `SUPABASE_PROJECT_REF` | GitHub Actions | `.github/workflows/edge-functions-deploy.yml` | GitHub repository variables (preferred) or secrets | `--project-ref` CLI target | On project change | Brian Lewis | GitHub repo settings → Secrets and variables → Actions | `docs/designs/2026-05-24-hygiene-pipeline-plan.md` |
| `SUPABASE_URL` | Next.js server | `/api/admin/executive/refresh` and server-side Supabase callers | Local `.env.local`, Netlify env | Base URL for functions and API | On project change | Brian Lewis | Netlify env UI + local `.env.local` check | `.env.example`; `docs/specs/PHASE1-OPS-VERIFICATION-RUNBOOK.md` |
| `SUPABASE_SERVICE_ROLE_KEY` | Next.js server | Server API routes and privileged Supabase clients | Local `.env.local`, Netlify env | Service role `Authorization` for privileged server-side Supabase access | Quarterly or on leak | Brian Lewis | Netlify env UI + local `.env.local` check | `.env.example`; `AGENTS.md`; `docs/specs/PHASE1-OPS-VERIFICATION-RUNBOOK.md` |
| `ANTHROPIC_API_KEY` | Next.js server and Supabase Edge | Approved AI/vision consumers, including Resident Record Intake when organization PHI policy allows | Local `.env.local`, Netlify env, Supabase Edge secrets as applicable | Provider API authentication; never browser-exposed | Quarterly, on leak, or provider rotation | Brian Lewis | Confirm secret name only in the target environment; run synthetic no-PHI parser smoke | `.env.example`; `supabase/functions/README.md`; `docs/specs/RESIDENT-RECORD-INTAKE.md` |
| `NEXT_PUBLIC_SENTRY_TRACES_SAMPLE_RATE` | Next.js browser | Sentry navigation performance traces | Netlify env (optional; defaults to `0.05` in production) | Decimal from `0` to `1`; contains no credential | On observability policy change | Brian Lewis | Inspect a sampled navigation transaction in Sentry | `.env.example`; `instrumentation-client.ts`; `docs/specs/OBSERVABILITY-SPEC.md` |
| `SENTRY_TRACES_SAMPLE_RATE` | Next.js server and edge | Sentry server/edge performance traces | Netlify env (optional; defaults to `0.05` in production) | Decimal from `0` to `1`; contains no credential | On observability policy change | Brian Lewis | Inspect a sampled server transaction in Sentry | `.env.example`; `sentry.server.config.ts`; `sentry.edge.config.ts`; `docs/specs/OBSERVABILITY-SPEC.md` |
| `TIMECLOCK_BADGE_HMAC_SECRET` | Next.js server | `/api/kiosk/timeclock/*` and `/api/admin/timeclock/credentials` (badge lookup key, spec 37) | Netlify env; optional until badges are rolled out (employee number and PIN work without it) | HMAC-SHA256 key over the badge value; rotation invalidates every registered badge and requires a re-scan | On leak or badge reissue | Brian Lewis | Register a badge on a synthetic staff record and punch with it on the kiosk | `docs/specs/37-timeclock.md`; `docs/operations/timeclock-kiosk-lockdown.md` |
| `CARE_EVENT_DISPATCHER_SECRET` | Supabase Edge, pg_cron/Vault | `care-event-dispatcher` (Something Happened delivery drain, spec 07A) | Supabase Edge secrets, Supabase Vault for the cron job | `x-cron-secret` | Quarterly or on leak | Brian Lewis | `supabase secrets list` | `supabase/functions/README.md`; `docs/specs/07A-something-happened-capture.md` |
| `TWILIO_ACCOUNT_SID` | Supabase Edge | `care-event-dispatcher` SMS and voice (also read by `risk-nightly-scorer`) | Supabase Edge secrets only | Twilio Basic auth username | Quarterly, on leak, or Twilio rotation | Brian Lewis | `supabase secrets list` | `supabase/functions/README.md`; `docs/specs/07A-something-happened-capture.md` |
| `TWILIO_AUTH_TOKEN` | Supabase Edge | `care-event-dispatcher` SMS and voice (also read by `risk-nightly-scorer`) | Supabase Edge secrets only | Twilio Basic auth password | Quarterly, on leak, or Twilio rotation | Brian Lewis | `supabase secrets list` | `supabase/functions/README.md`; `docs/specs/07A-something-happened-capture.md` |
| `TWILIO_FROM_NUMBER` | Supabase Edge | `care-event-dispatcher` SMS and voice sending number | Supabase Edge secrets only | Twilio `From` form field; contains no credential | On number change | Brian Lewis | `supabase secrets list` | `supabase/functions/README.md`; `docs/specs/07A-something-happened-capture.md` |
| `CARE_EVENT_SMS_ENABLED` | Supabase Edge | `care-event-dispatcher` channel switch for SMS and voice | Supabase Edge secrets only | Literal `true` enables Twilio sending; any other value or unset keeps sms and voice rows `skipped/channel_not_enabled`. Stays unset until the Twilio BAA is signed (spec 07A decision D5). | On BAA signature, then on policy change | Brian Lewis | `supabase secrets list` | `supabase/functions/README.md`; `docs/specs/07A-something-happened-capture.md` |

### Report schedule execution (September review remediation)

- `REPORT_SCHEDULER_SECRET`: shared secret on the Next.js server and report-scheduler Edge Function.
- `REPORT_SCHEDULER_RUNNER_URL`: Edge-only HTTPS URL to the deployed Next.js `/api/reports/scheduler` endpoint. This is configuration, not a secret.
- Missing configuration fails closed; recurrence is not enabled by source changes alone.

### Private workforce publisher (COL-721)

- `WORKFORCE_PUBLISHER_CRON_SECRET`: Haven Edge-only invocation secret; its corresponding dedicated Vault value is `workforce_publisher_cron_secret`. It authorizes only the workforce publisher's empty-body POST.
- `WORKFORCE_INGEST_SECRET`: Haven Edge-only HMAC secret for Front Office's private workforce roster receiver, bound to the verified Haven organization and `workforce_roster` dataset. It grants no reads or source database access and must not reuse Stand Up, officer catalog or another source's key.
- Both are absent by default; deployment does not activate the feed. Brian owns secret rotation and activation. Read `supabase/functions/workforce-publisher/README.md` for exact project, receipt and disabled-cron readback; inspect secret names only with `supabase secrets list --project-ref manfqmasfqppukpobpld`.
