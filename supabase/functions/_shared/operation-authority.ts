// Authority classes projected by public.operation_automation_tasks (migration 472).
// Site-level work only: protected subject classes never reach automation, so a
// population containing them fails the coverage check instead of scoring clear.
export const SITE_AUTHORITY_CLASSES = ["facility", "asset"];
